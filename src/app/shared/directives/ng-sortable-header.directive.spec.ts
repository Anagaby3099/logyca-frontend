import { NgSortableHeaderDirective } from './ng-sortable-header.directive';

describe('NgSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
